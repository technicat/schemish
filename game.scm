(define-class <game> () ())

(define-class <boardgame> () ())

(define-class <tictactoe> (<boardgame>) ())

(define-class <othello> (<boardgame>) ())

(define-class <checkers> (<boardgame>) ())

(define-class <chess> (<boardgame>) ())

(define-class <chinesechess> (<boardgame>) ())